# Draw method (summary)

1. One clear question.
2. Choose spread (default three-card).
3. Run `scripts/draw.mjs` or accept user-provided cards with orientation.
4. Fetch each card page from Wiki by `id`.
5. Read position meaning + upright/reversed against the question.
